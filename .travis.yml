# Sample .travis.yml for R projects

language: r
warnings_are_errors: false
sudo: required

env:
 global:
   - CRAN: http://cran.rstudio.com

r_packages:
   - httr
   - jsonlite
   - testthat
   - knitr
notifications:
  email:
    on_failure: change
